@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(Threads)

if(@WITH_PYTORCH@)
    find_dependency(Torch)
endif()

if(@WITH_BLENDER@)
    find_dependency(OpenGL)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/Buildify3DGSTargets.cmake")

check_required_components(Buildify3DGS)